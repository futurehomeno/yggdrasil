/// The scroll related [YgLayout] header behavior.
enum YgHeaderBehavior {
  /// Keep the header fixed at the top of the screen.
  fixed,

  /// Allow the user to scroll the appBar off the screen by scrolling down.
  hideAppBar,

  /// Allow the user to scroll the entire header off the screen by scrolling
  /// down.
  hideEverything,
}
