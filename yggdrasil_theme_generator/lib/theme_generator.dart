import 'dart:io';

class ThemeGenerator {
  ThemeGenerator();

  void generateTheme({
    required String tokenDirectory,
    required String themeDirectory,
    required bool ignoreErrors,
  }) {
    print(Directory.current);
  }
}
