/// Predefined sizes for the tiles of a [YgTileSelector].
///
/// The size determines the icon and its circle, the label style and the
/// padding of the tiles, similar to how [YgButtonSize] works for buttons.
enum YgTileSelectorSize {
  small,
  medium,
  large,
}
