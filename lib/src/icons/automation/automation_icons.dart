class AutomationIcons {
  const AutomationIcons._();

  static const String _path = 'automation/';

  static const String device = '${_path}device';
  static const String disabled = '${_path}disabled';
  static const String door = '${_path}door';
  static const String duplicate = '${_path}duplicate';
  static const String energy = '${_path}energy_25';
  static const String humid = '${_path}humid';
  static const String energyPig = '${_path}energy_pig_25';
  static const String london = '${_path}london';
  static const String motion = '${_path}motion';
  static const String notification = '${_path}notification';
  static const String play = '${_path}play';
  static const String scene = '${_path}scene';
  static const String sensor = '${_path}sensor';
  static const String shortcuts = '${_path}shortcuts';
  static const String sunrise = '${_path}sunrise';
  static const String sunset = '${_path}sunset';
  static const String temp = '${_path}temp';
  static const String time = '${_path}time';
  static const String value = '${_path}value';
  static const String watt = '${_path}watt';
  static const String window = '${_path}window';

  static const Map<String, String> map = <String, String>{
    'device': device,
    'door': door,
    'duplicate': duplicate,
    'energy': energy,
    'humid': humid,
    'london': london,
    'modes': shortcuts,
    'motion': motion,
    'notification': notification,
    'play': play,
    'scene': scene,
    'sensor': sensor,
    'sensor_contact': sensor,
    'sensor_motion': sensor,
    'shortcuts': shortcuts,
    'sunrise': sunrise,
    'sunset': sunset,
    'temp': temp,
    'time': time,
    'value': value,
    'watt': watt,
    'wattage': sensor,
    'window': window,
  };
}
