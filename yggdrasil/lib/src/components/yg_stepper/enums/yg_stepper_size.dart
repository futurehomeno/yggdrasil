/// Predefined sizes for a [YgStepper].
///
/// The size determines the height of the stepper, the size of the add and
/// minus buttons and the text styles of the value and metric, similar to how
/// [YgButtonSize] works for buttons.
enum YgStepperSize {
  small,
  medium,
  large,
}
